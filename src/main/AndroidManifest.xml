<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smart_bus_system">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_bus"

        android:label="Smart Bus"
        android:roundIcon="@drawable/logo_bus"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smart_bus_system"
        android:usesCleartextTraffic="true">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Passenger_k" />
        <activity android:name=".HomePage_k" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".Bus_Location"
            android:label="@string/title_activity_bus__location" />
        <activity android:name=".Message_k" />
        <activity android:name=".TimeKeeper.TimeKeeperMainActivity" />
        <activity android:name=".BusOwner.DriverMainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".User.UserMainNav" />
        <activity android:name=".qrcode.activities.SetupActivity" />
        <activity
            android:name=".qrcode.activities.PassengerDashboard"
            android:label="@string/title_activity_passenger_dashboard"
            android:theme="@style/Theme.QrCode.NoActionBar" />
        <activity
            android:name=".qrcode.activities.BusDriverDashboard"
            android:label="@string/title_activity_drawer"
            android:theme="@style/Theme.QrCode.NoActionBar" />
        <activity android:name=".qrcode.activities.RegisterActivity" />
        <activity android:name=".qrcode.activities.SplashScreen" />
        <activity android:name=".qrcode.activities.LoginScreen" />
        <activity android:name=".qrcode.activities.Scanner" />
    </application>

</manifest>